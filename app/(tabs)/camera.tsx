import { Text, View } from "react-native";

export default function Camera() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-sky-500"> This is the Camera page.</Text>
    </View>
  );
}
